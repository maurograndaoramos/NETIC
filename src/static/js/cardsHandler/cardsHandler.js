const cardsContainer = document.querySelector(".cards")

const cardsHandler = () => {
    document.querySelector("body").addEventListener("changedCourse", (event) => {
        const course = event.detail.course.trim()
        
    })
}


cardsHandler()